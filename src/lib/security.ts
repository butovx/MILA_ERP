/**
 * Пустой файл безопасности
 *
 * Оставлен для обратной совместимости
 * Вся логика SSL-обработки перенесена в db.ts
 */

export default function setupSecurity() {
  console.log("SSL безопасность теперь настроена в db.ts");
  return true;
}
